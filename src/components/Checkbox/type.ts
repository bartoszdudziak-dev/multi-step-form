import { Variant } from 'styled-components';

export type CheckboxProps = {
    variant?: Variant;
    label?: string;
    id?: string;
};

export type StyledCheckboxProps = {
    $variant: Variant;
};
